package design.parking.service;

import design.parking.entity.BaseCar;
import design.parking.entity.BaseParkingPlace;

public class ParkingServiceImpl implements ParkingService {
    BaseParkingPlace parkingPlace = new BaseParkingPlace();

    @Override
    public int addCar(BaseCar car) {

        return 0;
    }

    @Override
    public int removeCar(BaseCar car) {
        return 0;
    }
}
